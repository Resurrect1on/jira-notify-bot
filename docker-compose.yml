services:
  jira-bot:
    build: .
    image: n3v3rlucky/jira-bot:1.4.0
    container_name: jira-bot
    restart: always
    env_file:
      - .env
    ports:
      - "5000:5000"
    command: uvicorn src.main:app --host 0.0.0.0 --port 5000